name: senior_observability
description: "Senior Observability Package — Firebase, Sentry & Clarity integration for Flutter apps."
version: 1.0.0
homepage:

resolution: workspace

environment:
  sdk: ^3.11.4
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.7.0
  firebase_analytics: ^12.3.0
  firebase_crashlytics: ^5.2.0
  firebase_performance: ^0.11.3
  sentry_flutter: ^9.17.0
  clarity_flutter: ^1.8.0
  logger: ^2.7.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  very_good_analysis: ^10.2.0
  mocktail: ^1.0.5

flutter:
